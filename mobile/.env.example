# Mobile configuration is passed via `--dart-define` at build time, not
# from a .env file. The supported keys are listed below; use them with:
#
#   flutter run \
#     --dart-define=API_BASE_URL=https://api.nutritrack.app/api \
#     --dart-define=SENTRY_DSN=https://...@sentry.io/...

API_BASE_URL=http://10.0.2.2:3000/api
SENTRY_DSN=
